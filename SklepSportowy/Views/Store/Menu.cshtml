@model IEnumerable<SklepSportowy.Models.Category>

<nav class="categories-left">
    <ul>
        @foreach(var cat in Model)
        {
            <li>
                <a href="@Url.Action("List", "Store", new { currentCategory = cat.Name }) ">
                    <img src="~/Content/Icons/@cat.IconFilename" />
                    @cat.Name
                </a>
            </li>
        }
    </ul>
</nav>